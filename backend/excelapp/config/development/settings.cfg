import os
from uuid import uuid4
from datetime import timedelta




SECRET_KEY = str(uuid4())

base_dir = os.path.dirname(__file__)
database_path = os.path.join(base_dir, 'data.sqlite')
SQLALCHEMY_DATABASE_URI = f'sqlite:///{database_path}'


SESSION_TYPE = 'sqlalchemy'
SESSION_COOKIE_SECURE = True # httpsの使用
SESSION_COOKIE_HTTPONLY = True # JavaScriptからクッキーにアクセスできないようにします
SESSION_COOKIE_SAMESITE = 'Lax' # クロスサイトリクエストでクッキーが送信されないようにします
PERMANENT_SESSION_LIFETIME = timedelta(minutes=30)

OAUTHLIB_INSECURE_TRANSPORT = '1' #HTTPでのOAuth認証を許可します（開発環境用）
OAUTHLIB_RELAX_TOKEN_SCOPE = '1' #トークンスコープの厳密な一致を緩和します

GOOGLE_CLIENT_ID = "10276624078-4f2jjf0hpm8cvhr53mb9ksj5lnmk2hll.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET = "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"

UPLOAD_FOLDER = './uploads'
MAX_CONTENT_LENGTH = 16 * 1000 * 1000
SCHEDULER_API_ENABLED= True
ENV='development'