顧客情報が全部入ったExcelファイルを持ってくる

excelファイルをアップロード
複数ファイルをアップロード
(google colabotoryでやったようにいくつかのファイルをアップロードしてconcatする)

pandasのdataframe形式で読み取る



ilocを使って
列を削ぎ落とす/順番を帰る
コラムの日付形式を変える
列をタブにして
タブをつまんで並べ変える（JavaScript?）

(機能1,2,3はラジオボタンで選べる)

機能1:{指定したコラム(列)}にある氏名でワークシート/ブック作る

機能2:ワークシート/ブックに　氏名ごと　データを行ごと移植する

その際、とってくる列の順番をチェックを入れブラウザで選択する　
0,1,2,3,4,

氏名,日付,回数,教科1,教科2,教科3,・・・

機能3(オプション):作成したシートを選択したカラムの降順/昇順　で整頓する

機能4:作成したシートまたはブックをグラフにする

その際、グラフの色

印刷設定
def print():
    import openpyxl


wb = openpyxl.load_workbook('sample.xlsx')

for ws in wb.worksheets:
    ws.page_setup.orientation = "portrait"
    ws.page_setup.fitToWidth = 1  # 横1ページ
    ws.page_setup.fitToHeight = 0  # 縦は指定なし
    ws.sheet_properties.pageSetUpPr.fitToPage = True
wb.save('print.xlsx')